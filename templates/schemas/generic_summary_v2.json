{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tóm tắt chung V2",
  "description": "Định dạng tóm tắt chung có cấu trúc với các thẻ phân loại",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Một tiêu đề ngắn gọn, phù hợp, nắm bắt được bản chất của nội dung",
      "maxLength": 200
    },
    "summary": {
      "type": "string",
      "description": "Tóm tắt toàn diện về nội dung",
      "maxLength": 2000
    },
    "key_topics": {
      "type": "array",
      "description": "Các chủ đề hoặc chủ đề chính được xác định trong nội dung",
      "items": {
        "type": "string",
        "maxLength": 200
      },
      "minItems": 1,
      "maxItems": 15
    },
    "tags": {
      "type": "array",
      "description": "Thẻ danh mục để phân loại nội dung (FR-6)",
      "items": {
        "type": "string",
        "maxLength": 50
      },
      "minItems": 1,
      "maxItems": 10
    }
  },
  "required": ["title", "summary", "key_topics", "tags"],
  "additionalProperties": false
}
